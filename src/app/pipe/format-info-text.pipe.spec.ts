import { FormatInfoTextPipe } from './format-info-text.pipe';

describe('FormatInfoTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatInfoTextPipe();
    expect(pipe).toBeTruthy();
  });
});
